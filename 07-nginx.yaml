- name: install and run nginx
  hosts: mongodb
  tasks:
  become: yes
  - name: install nginx
    ansible.builtin.yum:
     name: nginx
     state: installed
  - name: nginx started
    ansible.builtin.service:
     name: nginx
     state: started